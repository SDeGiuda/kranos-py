# Core detection (YOLOv8)
ultralytics>=8.0.0

# HTTP client for cloud upload
requests>=2.31.0

# YAML configuration parsing
PyYAML>=6.0

# Image handling (used by detector and capture fallback)
Pillow>=10.0.0

# Raspberry Pi camera (install only on RPi)
# picamera2>=0.3.12

# Temperature/humidity sensor (install only on RPi)
# adafruit-circuitpython-dht>=3.7.0

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0
